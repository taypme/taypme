### taypme: Full Stack Inventor/Musician
#### The Church of Jesus Christ of Latter Day Saints = TRUE

I am a **Drupal/Symfony/PHP** engineer building multi-tenant **Symfony 8** runtimes, **Drupal 11** control layers, and C11 tooling inside Dockerized workflows. I work across Symfony components, Twig, Doctrine, PSR-7/PSR-15 middleware, **MySQL**, Redis, Mercure, and native C to make routing, caching, code generation, and deployment predictable. Huge **Node.js** fan.

---

I like software that can *explain* itself. In practice that means turning hard problems into explicit systems: domain/subdomain/endpoint- aware request resolution, package discovery and module wiring, environment and asset resolution, database-backed configuration, source import/export pipelines, and deterministic rebuilds. In [Symfonicat](https://symfonicat.com), that shows up as a **Symfony 8** multi-tenant frontend runtime with Twig templates, PSR-15 middleware, Doctrine-backed state, Composer-driven package discovery, and support for native **PHP** extensions and Go modules when the problem needs lower-level code. In srchorse, it becomes a **PHP/MySQL** corpus toolchain that tokenizes php-src, stores exact source pieces, reconstructs files, and validates the result. In gizmo, it is PHP/Twig generating tiny C programs ahead of time so repeated work can be cached instead of recomputed. In forensic, it is a C11 sweeper that hashes, relocates, categorizes, deduplicates, and refactors repositories at scale.

---

I also keep a running idea map for repository automation, runtime/compiler experiments, security, and infrastructure. That is where I think about semantic refactors, change budgeting, namespace drift, path aliasing, reproducible builds, and other systems that help large codebases stay maintainable. I am most interested in work where **PHP** and **C** meet the filesystem, the database, the build pipeline, and the runtime, because that is where the leverage tends to be highest.

That pattern carries across my work: use **Symfony** and **PHP** when the application layer is the right tool, drop to C when performance or control matters, and use Docker to keep the whole stack reproducible and easy to reason about. **Check out my projects and hit me up!**
